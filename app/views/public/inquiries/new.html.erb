<h2>問い合わせフォーム</h2>
<%= form_with model:@inquiry, url: confirm_inquiries_path, local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>問い合わせ内容</h4>
  <%= f.text_area :body %>
  <%= f.submit '確認画面へ進む' %>
<% end %>